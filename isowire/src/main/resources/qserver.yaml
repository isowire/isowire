# QServer Configuration

server:
  port: 9999
  threadPoolSize: 10

# Channel configurations
channels:
  - name: "test-channel"
    type: "client"
    host: "localhost"
    port: 9999

# Logging configuration
logging:
  level: INFO
  file: "logs/qserver.log"
